# Imagerangefinder
Python based rangefinder
It uses 2 images (taken a known distance apart and with a camera with known aperture) to estimate distance based on the pixel difference between the images


## Usage
Save the 2 images(taken by cameras with the same FOV) to a subfolder "images/".
Run `main.py` and enter the parameters when prompted.
